<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="cuzki.chartgraphy.DataAnalyzedActivity">


    <android.support.design.widget.TabLayout
        android:id="@+id/tab_fragment_title"
        android:layout_width="match_parent"
        android:layout_height="38dp"
        android:background="@android:color/white"
        app:tabIndicatorColor="@color/co_primary_color"
        app:tabSelectedTextColor="@color/co_primary_color"
        app:tabIndicatorHeight="1dp"
        app:tabTextColor="@color/co_tab_txt_color"
        />

    <FrameLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/tab_fragment_title">
    </FrameLayout>

    <FrameLayout
        android:id="@+id/landContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        >
    </FrameLayout>
</RelativeLayout>
